```mermaid
graph TD
    A[overview] --> B[LLM模型]
    B --> C[Basic LLM]
    B --> D[Instruction Tuned LLM(指令)]
    D --> E[基于文本指令调优]
    D --> F[基于工具和服务使用和优化定制]
    D --> G[规则引导 prompt]
    D --> H[基于 basic llm 进行微调]
    D --> I[RLHF 技术(人类反馈中间对齐)]
    A --> J[工具管理提示生成]
    J --> K[Clear 不要写 short]
    K --> L[删除：作为例子的行为]
    K --> M[删除：避免 prompt injections]
    K --> N[删除：prompt 重组或增强任务]
    N --> O[除非有 if else]
    O --> P[调用API 时避免注射：确保注射被覆盖指定新的prompt]
    J --> Q[Guidelines]
    Q --> R[prompting Principles]
    R --> S[P1: 编写高精度明确的提示]
    S --> T[清楚解释任务和目标]
    T --> U[例子：给出一个明确的任务或目标的例子 prompt]
    U --> V[任务：回答问题，并以一致的风格]
    V --> W[<child>: 教我关于耐心的内容。]
    V --> X[<grandparent>: 雪花般的溪流起源于一个卑微的泉眼；最精妙的交响曲源自一个简单的音符。]
    V --> Y[<child>: 教我关于韧性的内容。]
    U --> Z[prompt-输入]
    Z --> AA[执行以下操作： - 用一个句子总结以下文本，用 `->` 分隔段落。 1. 将名称翻译成法语。 2. 在法语总结中使用翻译的名称。 3. 输出一个包含以下内容的 json 对象： keys: french_summary, num_names. 用换行符分隔答案。 文本： "<text>"]
    S --> AB[P2: 指导模型修改 prompt 的结构和格式]
    AB --> AC[可以是自定义提示或系统修正]
    AB --> AD[根据自己的风格，模型会被指导达到预期的输出]
    AB --> AE[示例：通过重组文本任务以便更清晰地理解，使用新的输入框架]
    AE --> AF[任务：输出总结并翻译成法语]
    AF --> AG[文本："<text>"]
```
